% ~250-350 words. Write this LAST, once the results exist.
% Structure that works: (1) knowledge graphs evolve and nobody can see how;
% (2) what this thesis builds; (3) how it is evaluated; (4) the headline numbers.
% Headline numbers already available: the integer-encoded diff is 6.0x faster and
% uses 16.5x less memory than the naive string baseline.

% TODO: write the abstract.
